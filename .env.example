# Base de datos MySQL
DB_HOST=localhost
DB_USER=tu_usuario
DB_PASS=tu_password
DB_NAME=lms_cenat

#Servidor
PORT=3000
NODE_ENV=development

# Sesión: generar con `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
# NUNCA usar un valor de ejemplo/placeholder en producción.
SESSION_SECRET=
